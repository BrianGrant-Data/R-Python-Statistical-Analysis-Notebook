# R Packages to install before working
install.packages(c("mosaic", "plotly", "tidyverse", "dplyr"))

# R Packages of data sets
install.packages(c("ggplot2movies"))
  # You could try "nycflights13" as well if you